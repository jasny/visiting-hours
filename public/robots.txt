# robots.txt to restrict indexing to only the homepage and the example page
User-agent: *

# Block user-generated content pages
Disallow: /page/

# Block Next image optimizer ONLY when it proxies external images (user uploads)
Disallow: /_next/image*url=https://*
Disallow: /_next/image*url=http://*

